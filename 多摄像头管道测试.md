# 多摄像头管道测试

本文记录本次在 `Capstone` 项目中运行多摄像头管道的完整过程（含无拓扑版、拓扑版、结果对比）。

## 1. 测试目标

- 输入目录：`/home/yangxp/Capstone/video_data/Mul_camera/Person4_14-20-14-25`
- 输出目录：`/home/yangxp/Capstone/test_mulcamera`
- 目标：
  - 跑通多摄像头 pipeline
  - 保存 JSON 结果
  - 开启摄像头拓扑后再跑一遍并对比

## 2. 运行环境

- Python 环境：`conda activate capstone`
- 运行设备：GPU（`reid_device="cuda"`）
- 入口函数：`video.factory.multi_camera_coordinator.run_multi_camera_pipeline`

## 3. 输入数据检查

输入目录下共 8 路 `.avi`：

- `2018-03-11.14-20-01.14-25-01.bus.G505.r13.avi`
- `2018-03-11.14-20-01.14-25-01.bus.G506.r13.avi`
- `2018-03-11.14-20-01.14-25-01.bus.G508.r13.avi`
- `2018-03-11.14-20-01.14-25-01.school.G328.r13.avi`
- `2018-03-11.14-20-01.14-25-01.school.G339.r13.avi`
- `2018-03-11.14-20-01.14-25-01.school.G419.r13.avi`
- `2018-03-11.14-20-01.14-25-01.school.G421.r13.avi`
- `2018-03-11.14-20-01.14-25-01.school.G423.r13.avi`

运行时按文件名排序并自动映射：

- `cam1` ~ `cam8`

## 4. 第一次运行（基线，无显式拓扑路径）

使用以下方式运行（核心参数）：

- `model_path="n"`
- `tracker="botsort_reid"`
- `conf=0.25`
- `iou=0.45`
- `reid_device="cuda"`

产物：

- `/home/yangxp/Capstone/test_mulcamera/multicam_output_person4_14-20-14-25.json`

## 5. 第二次运行（开启拓扑）

在同样参数下，额外传入：

- `topology_prior_path="/home/yangxp/Capstone/test_mulcamera/camera_topology_person4_14-20-14-25.json"`

说明：

- 若该文件不存在，则按当前相机列表初始化拓扑先验；
- 跨摄像头匹配时启用融合打分（默认权重）：
  - `score = 0.55 * cosine + 0.45 * topology_score`
- 运行结束后将拓扑先验持久化到该路径。

产物：

- `/home/yangxp/Capstone/test_mulcamera/multicam_output_person4_14-20-14-25_with_topology.json`
- `/home/yangxp/Capstone/test_mulcamera/camera_topology_person4_14-20-14-25.json`

## 6. 结果健康检查

两次运行均成功产出 JSON，结构正常，可解析。关键顶层字段：

- `meta`
- `global_entities`
- `events`

## 7. 基线 vs 拓扑 量化对比

对比文件：

- 基线：`multicam_output_person4_14-20-14-25.json`
- 拓扑：`multicam_output_person4_14-20-14-25_with_topology.json`

对比结果（本次数据）：

- `events`：`1551 -> 1551`
- `global_entities`：`1 -> 1`
- `events_with_global_entity_id`：`165 -> 165`
- `global_id_coverage`：`10.64% -> 10.64%`
- 分相机事件分布：一致（无变化）

结论：本次“开启拓扑”在当前数据和参数下未改变最终匹配结果。

## 8. 本次执行流程（可复现）

1. 准备环境：`conda activate capstone`
2. 检查输入目录与输出目录
3. 组装 `camera_videos`（按排序映射 `cam1~cam8`）
4. 跑基线版本并保存 JSON
5. 传 `topology_prior_path` 再跑一版
6. 检查输出文件存在性与可解析性
7. 统计对比关键指标（events、entities、global_id 覆盖率）

## 9. 下一步建议

若目标是提高跨镜头关联覆盖率，优先做阈值实验（A/B/C）：

- 下调 `cross_camera_min_score`
- 下调 `embedding_threshold`
- 调整 `topology_weight_reid/topology_weight_topo`
- 视场景增大 `max_transition_sec`