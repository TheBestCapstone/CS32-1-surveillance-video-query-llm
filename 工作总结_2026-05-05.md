# 工作总结（2026-05-05）

## 1. Git 分支同步与整理

- 当前分支 `cursor/消融实验` 与 `main` 完成同步确认：`main` 与 `cursor/消融实验` 一致。
- 已将 `cursor/消融实验` 的内容 fast-forward 合并到 `main` 并推送到远端 `origin/main`。
- 根据需求删除了以下分支（本地 + 远端）：
  - `cursor/消融实验`
  - `cursor/p1-next-a-summary-bailout-and-p1-3-embedding-cache`
- 创建并切换到新分支：`多摄像头分支`。

## 2. 多摄像头 Pipeline 执行

- 数据目录：`/home/yangxp/Capstone/video_data/Mul_camera/Person4_14-20-14-25`
- 输入视频：8 路 `.avi` 文件（自动映射为 `cam1` ~ `cam8`）。
- 运行环境：`conda activate capstone`
- 最终执行方式：GPU 模式（`reid_device=\"cuda\"`）成功完成。
- 输出目录：`/home/yangxp/Capstone/test_mulcamera`
- 输出文件：`multicam_output_person4_14-20-14-25.json`

## 3. 结果健康检查（是否正常）

- JSON 结构可正常解析，顶层字段完整：
  - `meta`
  - `global_entities`
  - `events`
- `meta` 中包含：
  - 8 路相机映射 `cameras`
  - 交叉匹配配置 `cross_camera_config`
- 任务过程中无结构性报错，结果文件成功生成。

## 4. 量化分析结果

- `events_total`：1551
- `entities_total`：1
- `events_with_global_id`：165
- `events_with_global_id_ratio`：10.64%
- 时间范围：`0.0s ~ 300.03s`
- 单个全局实体 appearance 数：73

### 分相机事件量占比

- `cam4`：675（43.52%）
- `cam5`：382（24.63%）
- `cam2`：346（22.31%）
- `cam3`：78（5.03%）
- `cam1`：57（3.68%）
- `cam8`：9（0.58%）
- `cam6`：2（0.13%）
- `cam7`：2（0.13%）

### 分相机已链接事件（带 `global_entity_id`）

- `cam1`：2
- `cam2`：55
- `cam3`：12
- `cam4`：5
- `cam5`：79
- `cam6`：2
- `cam7`：1
- `cam8`：9

## 5. 结论与下一步建议

- 当前多摄像头流程已跑通，产物有效。
- 跨镜头关联覆盖率（10.64%）偏低，仍有提升空间。
- 建议下一步进行参数调优实验（A/B/C 三组），优先调整：
  - `cross_camera_min_score`
  - `embedding_threshold`
  - `topology_weight_reid` / `topology_weight_topo`
  - `max_transition_sec`